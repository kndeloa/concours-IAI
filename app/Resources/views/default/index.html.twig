{% extends 'layout.html.twig' %}

{% block content %}
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}

      {{ include('dashboard/index.html.twig') }}
      
    {% else %}

      {{ include('default/landing-page.html.twig') }}
      
    {% endif %}
{% endblock %}
