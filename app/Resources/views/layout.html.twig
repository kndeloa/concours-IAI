{% extends 'base.html.twig' %}

{% block body %}

    <nav class="navbar">

        <div class="container">

            <div class="navbar__brand">
                <a href="{{ path('homepage') }}">Concours IAI</a>
            </div>

            <ul class="navigation">
                <li class="navigation__item">
                    <a href="{{ path('homepage') }}">Home</a>
                </li>
                <li class="navigation__item">
                    <a href="{{ path('homepage') }}">Login</a>
                </li>
                <li class="navigation__item">
                    <a href="{{ path('homepage') }}">Register</a>
                </li>
            </ul>


        </div>

    </nav>

    {% block content %}{% endblock %}

{% endblock %}